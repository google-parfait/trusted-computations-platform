# The default SignatureBindingVerifier does not provide an INFO string; update
# the InstanceSessionBinder to match.
#
# It's likely that we'll want a non-empty INFO string in the future, but this
# fix requires fewer Oak changes.
--- oak_sdk/containers/orchestrator_crypto_client.rs
+++ oak_sdk/containers/orchestrator_crypto_client.rs
@@ -28 +28 @@
-const SESSION_BINDER_INFO_STRING: &str = "oak-session-binding-key";
+const SESSION_BINDER_INFO_STRING: &str = "";
